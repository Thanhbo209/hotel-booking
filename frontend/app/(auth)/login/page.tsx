import { LoginForm } from "@/features/auth/components/LoginForm";

const LoginPage = () => {
  return (
    <section className="">
      <LoginForm />
    </section>
  );
};

export default LoginPage;
